import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ratnagirifarms.app',
  appName: 'Ratnagiri Farms',
  webDir: 'public',
};

export default config;